<?php
if (!defined('_GNUBOARD_')) exit; // 개별 페이지 접근 불가

// 댓글 첨부파일 설정
$cmt_file_level = 2; // 설정된 레벨 이상만 사용 가능
$cmt_img_width = 400; // 썸네일 가로사이즈 설정
$cmt_image_use = 0; // 이미지 파일만 등록 (0 모든파일 허용, 1 이미지파일만 허용)
?>